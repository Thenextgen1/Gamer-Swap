import React from "react";
import { DesktopNav, MobileNav } from "../../components";

const Header = () => {
  return (
    <header>
      <DesktopNav />
      <MobileNav />
    </header>
  );
};

export default Header;
