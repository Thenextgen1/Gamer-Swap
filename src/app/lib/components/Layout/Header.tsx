import React from "react";
import { DesktopNav, MobileNav } from "..";

const Header = () => {
  return (
    <header>
      <DesktopNav />
      <MobileNav />
    </header>
  );
};

export default Header;
